<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cn.mapper.UserSubscriptionMapper">

    <!-- 添加关注 -->
    <insert id="insert">
        INSERT INTO user_subscriptions (follower_id, followee_id)
        VALUES (#{followerId}, #{followeeId})
    </insert>

    <!-- 取消关注 -->
    <delete id="deleteByFollowerAndFollowee">
        DELETE FROM user_subscriptions
        WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
    </delete>

    <!-- 检查是否已关注 -->
    <select id="countByFollowerAndFollowee" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM user_subscriptions
        WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
    </select>

    <!-- 查询关注者的用户 ID 列表 -->
    <select id="selectFollowersByFolloweeId" resultType="java.lang.Long">
        SELECT follower_id FROM user_subscriptions
        WHERE followee_id = #{followeeId}
    </select>

    <!-- 检查关注状态 -->
    <select id="isFollowing" resultType="java.lang.Boolean">
        SELECT COUNT(*) > 0 FROM user_subscriptions
        WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
    </select>

    <!-- 获取粉丝数 -->
    <select id="countFollowers" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM user_subscriptions
        WHERE followee_id = #{followeeId}
    </select>

    <!-- 获取关注数 -->
    <select id="countFollowing" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM user_subscriptions
        WHERE follower_id = #{followerId}
    </select>

    <!-- 分页获取粉丝列表（含用户基本信息） -->
    <select id="selectFollowersList" resultType="com.cn.VO.UserVO">
        SELECT u.id, u.username, u.user_type AS userType
        FROM user_subscriptions us
        JOIN users u ON us.follower_id = u.id
        WHERE us.followee_id = #{followeeId}
        ORDER BY us.created_at DESC
    </select>

    <!-- 分页获取关注列表（含用户基本信息） -->
    <select id="selectFollowingList" resultType="com.cn.VO.UserVO">
        SELECT u.id, u.username, u.user_type AS userType
        FROM user_subscriptions us
        JOIN users u ON us.followee_id = u.id
        WHERE us.follower_id = #{followerId}
        ORDER BY us.created_at DESC
    </select>

</mapper>
